# CPU-only dependencies for Cloud Run deployment (GPU work delegated to RunPod workers)
# Do NOT include: torch, torchvision, transformers, easyocr, fastembed-gpu, onnxruntime-gpu

# Web framework
fastapi>=0.135.2
uvicorn>=0.42.0

# RunPod serverless SDK
runpod>=1.7.0

# AI / LLM providers
openai>=2.28.0
groq>=1.1.2
deepgram-sdk>=6.1.1

# Vector DB
qdrant-client>=1.17.1

# Config + settings
pydantic-settings>=2.9.0
python-dotenv>=1.0.0

# Google integrations
google-api-python-client>=2.193.0
google-auth-httplib2>=0.3.0
google-auth-oauthlib>=1.3.0

# Flashcards
genanki>=0.13.1

# R2 / S3 storage
boto3>=1.34.0

# Utilities
tqdm>=4.67.3
langsmith>=0.1.0
huggingface-hub>=0.24.0
