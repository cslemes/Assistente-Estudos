DEEPGRAM_API_KEY=""
QDRANT_API_KEY=""
QDRANT_URL=""
HUGGINGFACE_TOKEN=""

# LLM provider: "groq" (default) or "openai"
LLM_PROVIDER="groq"
GROQ_API_KEY=""
GROQ_MODEL="openai/gpt-oss-120b"
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"

# Transcription provider: "deepgram" (default) | "openai" | "groq"
TRANSCRIPTION_PROVIDER="deepgram"
OPENAI_WHISPER_MODEL="whisper-1"
GROQ_WHISPER_MODEL="whisper-large-v3-turbo"

# S3-compatible Object Storage — videos (MinIO locally, R2/S3 in production)
# MinIO local defaults (matches docker-compose.yml):
STORAGE_ENDPOINT_URL="http://localhost:9000"
STORAGE_ACCESS_KEY_ID="minioadmin"
STORAGE_SECRET_ACCESS_KEY="minioadmin"
STORAGE_BUCKET_NAME="videos"
STORAGE_PUBLIC_URL="http://localhost:9000/videos"
# STORAGE_REGION="us-east-1"   # default; leave blank for MinIO

# Cloudflare R2 (slide thumbnails)
CLOUDFLARE_ACCOUNT_ID=""
CLOUDFLARE_R2_ACCESS_KEY_ID=""
CLOUDFLARE_R2_SECRET_ACCESS_KEY=""
CLOUDFLARE_R2_BUCKET_NAME=""
CLOUDFLARE_R2_PUBLIC_URL=""  # e.g. https://pub-xxx.r2.dev

LANGSMITH_API_KEY=""

# Path to the Downloads folder. Leave as "Downloads" for local dev (relative to CWD).
# Set to /app/Downloads when running via Docker without docker-compose (e.g. Cloud Run).
# DOWNLOADS_BASE="Downloads"

# Backend API key — required on all non-health routes when set.
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
# Must be identical in both variables (API checks it; frontend sends it).
BACKEND_API_KEY=""
VITE_BACKEND_API_KEY=""

# ─── Frontend — Firebase Authentication (Google Sign-In) ──────────────────────
# These go in frontend/.env (VITE_ prefix required for Vite to expose them).
# Get values from: Firebase Console → Project Settings → Your apps → Web app → Config
# Enable Google sign-in at: Authentication → Sign-in method → Google
# Add localhost to: Authentication → Settings → Authorized domains
VITE_FIREBASE_API_KEY=""
VITE_FIREBASE_AUTH_DOMAIN=""          # <project-id>.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=""
VITE_FIREBASE_STORAGE_BUCKET=""       # <project-id>.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=""
VITE_FIREBASE_APP_ID=""               # 1:<sender-id>:web:<hash>